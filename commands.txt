git clone https://github.com/SantiagoV04/qtm350-quiz01.git
cd qtm350-quiz01
touch commands.txt
git checkout -b "feature-update"
mkdir reports
touch reports/summary.md
echo "This document provides a summary of the project updates." >> summary.md
git add .
git commit -m "Add reports directory"
touch reports/file{1,2,3}.txt
git status
mv data/raw-data.csv data/input-data.csv
mkdir scripts/backup
ls -l scripts
cp scripts/*.py scripts/backup
rm docs/documentation.md
git status
git add .
git commit -m "Update project files"
touch .gitignore
echo "temp/" > .gitignore
echo "dataset0?.csv" >> .gitignore
cat .gitignore
git add .gitignore && git commit -m "Add .gitignore file"
git checkout main
git merge feature-update

